server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nebur89/Bootcamp-ConfigServer.git
        default-label: main
        search-paths:
        - 'reto-config-service*'
